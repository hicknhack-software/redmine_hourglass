.hidden.chronos-dialog.js-stop-dialog-content data-dialog-title=t('chronos.ui.issues.stop_dialog.title') data-button-ok-text=t(:button_apply) data-button-cancel-text=t(:button_cancel)
  p = t('chronos.ui.issues.stop_dialog.description')
  .center
    = collection_select :time_tracker, :activity_id, activity_collection(time_tracker.project), :id, :name, {required: true, include_blank: true}
