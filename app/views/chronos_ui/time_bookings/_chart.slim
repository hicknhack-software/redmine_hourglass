javascript:
  window.chronos.jqplotData = {
    data: #{raw @chart_data.to_json},
    ticks: #{raw @chart_ticks.to_json},
    highlightData: #{raw @highlight_data.to_json},
    hourSign: "#{t('chronos.ui.chart.hour_sign')}"
  };
= javascript_include_tag 'jqplot', plugin: 'redmine_chronos'
= stylesheet_link_tag 'jqplot/jquery.jqplot', plugin: 'redmine_chronos'
