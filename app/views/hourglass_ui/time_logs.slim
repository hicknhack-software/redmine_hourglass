- if policy(Hourglass::TimeLog).create?
  - content_for :contextual do
    = link_to sprite_icon("time-add", t('hourglass.ui.time_logs.button_create')), hourglass_ui_time_logs_new_path, class: 'icon js-create-record', remote: true, data: {type: 'html'}

= render template: 'hourglass_ui/query_view'
